<nav class="navbar navbar-expand-lg navbar-light bg-warning fs-3">
  <div class="container-fluid">
    <a class="navbar-brand fs-1" href="{{ path('app_main') }}">Flighter</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav">
        <li class="nav-item">
          <a class="nav-link" aria-current="page" href="{{ path('app_main') }}">Accueil</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="{{ path('app_flights_index') }}">Liste des vols</a>
        </li>
        {% if is_granted('ROLE_ADMIN') %}
        <li class="nav-item">
          <a class="nav-link" href="{{ path('app_flights_new') }}">Créer un vol</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="{{ path('app_cities_new') }}">Créer une ville</a>
        </li>
        {% endif %}
        
      </ul>
      <ul class="navbar-nav ms-auto">
      <li class="nav-item">
      {% if app.user %}
      <a href="{{path('app_logout')}}" class="nav-link">Se déconnecter</a>
      {% else %}
      <a href="{{path('app_login')}}" class="nav-link">Se connecter</a></li>
      <a href="{{path('app_register')}}" class="nav-link">S'inscrire</a>
      {% endif %}
        
      </ul>
    </div>
  </div>
</nav>